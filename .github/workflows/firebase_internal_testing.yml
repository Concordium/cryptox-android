name: Distribute builds for internal testing through Firebase

on:
  push:
    tags:
      - "*-qa.*"
      - "*-rc.*"
      - "*-oko.*"
    branches:
      - "feature/build_pipelines"
  workflow_dispatch:

env:
  java_version: "11"

jobs:
  build:
    runs-on: ubuntu-latest

    environment:
      name: Firebase-ConcordiumMobileWallet
      url: https://console.firebase.google.com/project/concordiummobilewallet/appdistribution/

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Create Firebase credentials file
        run: 'echo "$CREDENTIALS" > app/app-distribution-credentials.json'
        shell: bash
        env:
          CREDENTIALS: ${{secrets.GOOGLE_APPLICATION_CREDENTIALS}}

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: "${{env.java_version}}"
          distribution: corretto
          cache: gradle

      - name: Build and distribute Testnet release APK
        run: ./gradlew app:assembleTstnetRelease app:appDistributionUploadTstnetRelease --stacktrace

      #- name: Build Stagenet release APK
      #  run: ./gradlew app:assembleStagenetRelease --stacktrace

      #- name: Build Mainnet release APK
      #  run: ./gradlew app:assembleMainnetRelease --stacktrace

#      - name: Upload the APKs
#        uses: actions/upload-artifact@v3
#        with:
#          name: APKs
#          path: app/build/outputs/apk/**/*.apk
