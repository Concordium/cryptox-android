<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="CCX_Screen" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">
            <!-- This color is obtained experimentally.
            Figma value doesn't match the rendered background bottom color-->
            #0D0E12
        </item>
        <item name="android:windowBackground">@drawable/ccx_main_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <item name="materialAlertDialogTheme">@style/AlertDialogTheme</item>
    </style>
</resources>