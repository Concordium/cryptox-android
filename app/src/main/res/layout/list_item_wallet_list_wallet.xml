<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:layout_marginTop="8dp"
    android:background="@drawable/wallet_list_item_background">

    <TextView
        android:id="@+id/name_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="18dp"
        android:layout_marginTop="18dp"
        android:textAppearance="@style/CCX_Typography_H3"
        app:layout_constraintEnd_toStartOf="@id/indicator_radio_button"
        android:textColor="@color/cryptox_white_main"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/wallet_list_seed_wallet" />

    <TextView
        app:layout_constraintEnd_toStartOf="@id/indicator_radio_button"
        android:id="@+id/status_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="18dp"
        android:layout_marginBottom="18dp"
        android:textAppearance="@style/CCX_Typography_Body"
        android:textColor="@color/cryptox_white_main"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="@string/wallet_list_status_active_selected" />

    <RadioButton
        android:id="@+id/indicator_radio_button"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:clickable="false"
        android:layout_margin="32dp"
        android:background="@drawable/wallet_list_item_indicator"
        android:button="@null"
        android:checked="true"
        android:importantForAccessibility="no"
        android:src="@drawable/wallet_list_item_indicator"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
