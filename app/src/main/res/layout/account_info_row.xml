<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="10dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/accRoot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/btn_round_outline_bg"
        android:padding="24dp">

        <TextView
            android:id="@+id/accAddress"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="middle"
            android:maxLines="1"
            android:textAppearance="@style/MW24_Typography_Title"
            android:textColor="@color/cryptox_white_main"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Valera (sefergrgergergwergewrgwergergergerg)" />

        <TextView
            android:id="@+id/accIdentity"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:ellipsize="middle"
            android:maxLines="1"
            android:textAppearance="@style/MW24_Typography_Title_Light"
            android:textColor="@color/cryptox_white_main"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/accAddress"
            tools:text="Identity 1"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/accBalance"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textAppearance="@style/MW24_Typography_Title"
            android:textColor="@color/cryptox_white_main"
            app:layout_constraintStart_toStartOf="@id/accAddress"
            app:layout_constraintTop_toBottomOf="@id/accIdentity"
            tools:text="Balance: 2345670 CCD" />

        <TextView
            android:id="@+id/accBalanceAtDisposal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textAppearance="@style/MW24_Typography_Title_Light"
            android:textColor="@color/cryptox_white_main"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@id/accAddress"
            app:layout_constraintTop_toBottomOf="@id/accBalance"
            tools:text="At Disposal: 23456.70 CCD"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</LinearLayout>